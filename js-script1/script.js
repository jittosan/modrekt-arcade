import { Character } from "./statistic"
var character = new Character();

function moveRight() {
    //check characterPosition and adjust accordingly
}

function moveLeft() {
    //check characterPosition and adjust accordingly
}

function hitItem(item) {
    //if ((character.position == item.position) && item.hitGoal()) {
    //    character.updateScore(item);
    //}
}